services:
  bbp-scraper:
    build:
      context: ..
      dockerfile: .docker/Dockerfile
    container_name: bbp-domain-scraper
    env_file:
      - ../.env
    volumes:
      - ../:/app
    working_dir: /app
    command: ["python", "main.py", "--bc", "--h1", "--check-auth"]

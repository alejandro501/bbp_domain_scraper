# Config

Runtime configuration is environment-based only.

Preferred token variables in `.env`:
- `BC_TOKEN`
- `H1_TOKEN`
- `YWH_TOKEN`

Backward-compatible legacy names are still accepted:
- `BC_COOKIE`
- `H1_COOKIE`
- `YWH_PAT`

No JSON credential config is used.
